# IndexTTS2 Docker Compose (ARM64/GB10)

services:
  indextts2:
    platform: linux/arm64
    build:
      context: .
      dockerfile: Dockerfile.arm64
    ports:
      - "8002:8002"
    volumes:
      - ./checkpoints:/app/checkpoints
      - ./examples:/app/examples
      - ./assets:/app/assets
      - ./outputs:/app/outputs
    environment:
      - CUDA_VISIBLE_DEVICES=0
      - NVIDIA_VISIBLE_DEVICES=all
      - NVIDIA_DRIVER_CAPABILITIES=compute,utility
      - SKIP_MODEL_DOWNLOAD=1
    gpus: all
    shm_size: "8gb"
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8002/health"]
      interval: 30s
      timeout: 30s
      retries: 10
      start_period: 5m
